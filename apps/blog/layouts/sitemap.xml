{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
{{- /*
  Per-locale sitemap (overrides Hugo default).
  Filters:
    - Emit URLs for core 6 locales (en / zh-tw / zh-cn / ja / ko / it)
      + curated market locales with native, non-MT articles (de added 2026-05-22,
      country-specific concentrated-publishing pilot — NOT the blanket auto-translate
      R1 excluded; this locale ships hand-written native pages so it belongs in the
      canonical sitemap).
    - Skip listing hubs: /post/, /tags/, /categories/ (Kind = section / taxonomy).
    - Skip ALL term pages (Kind = term) — tightened 2026-05-08 to match
      head.html which now noindex's all term pages. Tag/category pages are
      navigation, not SERP landing; emitting them in sitemap while marking
      noindex creates a contradicting signal pair.
    - Always emit: home + post bundles only.
  Per spec seo-indexability-l2 FR-001 / FR-002.
*/ -}}
{{- $coreLocales := slice "en" "zh-tw" "zh-cn" "ja" "ko" "it" "de" "es" "pt-br" "pl" "tr" "fr" "th" "vi" -}}
{{- if in $coreLocales .Site.Language.Lang -}}
{{- range .Data.Pages -}}
  {{- $include := false -}}
  {{- if eq .Kind "page" -}}{{- $include = true -}}{{- end -}}
  {{- /* home: emit for core-6, but NOT for market locales de/es — their /de/ + /es/
         homes 301 to /en/ (CF bulk redirect, exact match); listing a redirecting URL
         in the sitemap creates a "Page with redirect" GSC notice. These locales ship
         only the article page(s). */ -}}
  {{- if eq .Kind "home" -}}{{- if not (in (slice "de" "es" "pt-br" "pl" "tr" "fr" "th" "vi") .Site.Language.Lang) -}}{{- $include = true -}}{{- end -}}{{- end -}}
  {{- /* Never list noindex / sitemap-disabled pages (e.g. /search/). This custom
         template overrides Hugo's native sitemap, so frontmatter
         `sitemap: disable: true` was being silently ignored — honor it here.
         Listing a noindex URL contradicts its robots meta (GSC reports it as
         "Excluded by 'noindex' tag" = sitemap noise). 2026-05-24. */ -}}
  {{- if .Sitemap.Disable -}}{{- $include = false -}}{{- end -}}
  {{- with .Params.robots -}}{{- if in (lower .) "noindex" -}}{{- $include = false -}}{{- end -}}{{- end -}}
  {{- if $include }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML (.Lastmod.Format "2006-01-02T15:04:05-07:00") }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}
  </url>
  {{- end -}}
{{- end -}}
{{- end -}}
</urlset>
